plugin:
  backend: go-plugin
  go-plugin:
    folder: ../plugins/bin/
ta-store:
  backend: memory
en-store:
  backend: memory
po-store:
  backend: memory
po-agent:
  backend: opa
vts-grpc:
  server:
    addr: vts:50051
